# ---- For training/train_qlora.py and training/inference.py (LocalFineTunedModel) ----
# Run these on Colab/Kaggle GPU, NOT on a CPU-only laptop.
torch>=2.2.0
transformers==4.44.2
peft==0.12.0
trl==0.9.6
bitsandbytes==0.43.3
accelerate==0.33.0
datasets

# ---- For agents/, eval/, demo/ (works on CPU / no GPU needed) ----
anthropic          # if using ApiModel(provider="anthropic") / API fallback
openai             # if using ApiModel(provider="openai") instead
gradio             # for demo/app.py
scipy              # optional, for judge-vs-human correlation in eval/judge.py

# ---- Note ----
# You do NOT need torch/transformers/peft/bitsandbytes installed to run agents/,
# eval/, or demo/ if you use --use_api_fallback — those imports are lazy and only
# trigger if you instantiate LocalFineTunedModel.
